#ifndef TYPES_H
#define TYPES_H

#include <array>
#include <vector>

#include "enums.h"

typedef unsigned long long U64;
typedef std::array<U64, 64> Movesets;
typedef std::vector<enumSquare> BlockerVector;

#endif
